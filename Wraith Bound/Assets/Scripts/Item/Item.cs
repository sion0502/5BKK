using UnityEngine;

public class Item : MonoBehaviour
{
    public enum Type { Active, Passive, Equip };
    public Type type;
    public int value;
    
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
